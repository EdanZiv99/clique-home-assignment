[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "checkin"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
checkin = "cli:main"
